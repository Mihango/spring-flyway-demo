CREATE TABLE IF NOT EXISTS STUDENT
(
    ID   LONG GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR(20),
    CLASS_NAME  VARCHAR(10)
);

create sequence student_sequence start with 1 increment by 1;

INSERT INTO STUDENT (ID, NAME, CLASS_NAME)
VALUES (1, 'JIMMY', '1S'),
       (2, 'RHYAN M', '1E');
